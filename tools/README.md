# Tools (Portfolio Automation)

These scripts were used to generate or validate portfolio artifacts.
They are not “evidence” and they do not contain secrets.

- create_grc_master.py: generates the workbook skeleton
- validate.py: checks required files exist
- generate_sdg_grc_portfolio.sh: generator (if retained)
